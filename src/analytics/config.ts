// Firebase configuration — fill these in from your Firebase console:
// https://console.firebase.google.com → Project Settings → Your apps → Web app
export const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};

// Admin password for the analytics dashboard.
// In production, move this to an environment variable or server-side auth.
export const ANALYTICS_PASSWORD = "djkim-admin-2026";
